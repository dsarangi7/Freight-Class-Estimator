# Agents — Freight Class Estimator

Static Astro freight class density estimator. See `OPENCODE.md` for handoff.

Hard rules:
- Density-only educational estimate; prominent disclaimers
- No AdSense / no empty ad slots
- GA4 only when `PUBLIC_GA4_ID` is set
- Keep `npm run test:golden` green
- Do not deploy unless explicitly asked
