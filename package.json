{
  "name": "freight-class-estimator",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:golden": "node --experimental-strip-types tests/golden.mjs",
    "pages:deploy": "npm run build && wrangler pages deploy dist --project-name=freight-class-estimator",
    "favicons": "node scripts/generate-favicons.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "wrangler": "^4.135.0"
  },
  "description": "Freight Class Estimator — NMFC-style density-based freight class estimator"
}
